<div class="container text-center position-absolute">
  <div class="form_area col-lg-7 mx-auto mt-5 p-5 position-relative ">
    <h2 class="mb-2 form_title">SIGN-UP</h2>
    <h3 class="mb-3 form_sub_title">新規登録</h3>
    <%= image_tag 'sign-up-icon.png', class: "mb-3" %>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="field">
        <%= f.text_field :name, autocomplete: "name", placeholder: "ニックネーム", class: "input_field" %>
      </div>
      <div class="field">
        <%= f.email_field :email, autocomplete: "email", placeholder: "メールアドレス", class: "input_field" %>
      </div>
      <div class="field">
        <%= f.password_field :password, autocomplete: "new-password", placeholder: "パスワード", class: "input_field" %>
      </div>
      <div class="field">
        <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "パスワード確認", class: "input_field" %>
      </div>
      <div class="actions">
        <%= f.submit "登録する", class: "form_btn" %>
      </div>
    <% end %>
  </div>
</div>
<%# 背景の読み込み %>
<%= render "shared/animated_background" %>
